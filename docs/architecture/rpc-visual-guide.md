# RPC Feature Visual Guide

Generated by examining the 45 screenshots captured by `tests/rpc-features.spec.ts`. Each image is shown with an honest description of what is visually present — including cases where the action produced no visible change.

---

## Navigation

### nav-multi-explorer.png

![Multi-explorer view](../screenshots/rpc/nav-multi-explorer.png)

The default landing view. A split-panel layout: the left sidebar shows a collapsible backend tree (substrate with 12 plugins, fidget-spinner with 1 plugin). The right panel shows detailed information about the selected `substrate` plugin — its children as clickable tags and a list of methods, with parameter input fields for the selected `call STREAM` method. Backend latency badges (`substrate 74ms`, `fidget-spinner 56ms`) appear at the top.

---

### nav-canvas.png

![Canvas (topology) view](../screenshots/rpc/nav-canvas.png)

**Nothing visually changed.** The display is identical to `nav-multi-explorer.png` — same backend tree, same plugin detail panel. The `ui.navigate` call targeted `canvas`, but the screenshot captures the explorer still rendered. The tab navigation may have fired but the content area did not visually switch.

---

### nav-sheet.png

![Sheet view](../screenshots/rpc/nav-sheet.png)

A mostly blank light-themed view. The left sidebar remains visible with the collapsed backend tree (substrate with 12 items, fidget-spinner with 1 item). The main content area is empty — the sheet view has no data to display in this state. This correctly shows the empty-state condition of the sheet view.

---

### nav-wiring.png

![Wiring canvas view](../screenshots/rpc/nav-wiring.png)

The wiring canvas view. A toolbar runs across the top with action buttons (`[+ Method]`, `[Run]`, `[X Clear]`, `[Export JSON]`, `[Auto Layout]`, etc.). The left sidebar lists searchable methods from both backends. The large center canvas shows placeholder text: *"Click to search & add nodes, or drag from the sidebar."* A TRANSFORMS section and UI component selector (Vars, Row, Col, Text, Input, Btn, Slider, Table) appear at the bottom of the sidebar.

---

### nav-orchestration.png

![Orchestration view](../screenshots/rpc/nav-orchestration.png)

The orchestration view in its initial empty state. A "WORKFLOWS" panel on the left shows "No workflows yet" with a `+ New` button. The main content area displays centered placeholder text: *"Select a workflow from the left, or click + New to create one."* The nav tab `orchestrate` is visually highlighted.

---

## Theming

### theme-daylight.png

![Daylight theme](../screenshots/rpc/theme-daylight.png)

The explorer view with the daylight (light) theme applied. Light gray backgrounds, muted blue accents, dark text. Plugin tags and method names are rendered in cool-blue tones. The layout and content are identical to the multi-explorer baseline — theme affects only colors, not structure.

---

### theme-midnight.png

![Midnight theme](../screenshots/rpc/theme-midnight.png)

The same explorer view with the midnight (dark) theme applied. Dark navy/near-black backgrounds, light blue accent tags, light gray text. The plugin tree and method detail panel are identical in structure to the daylight version, demonstrating a full color-scheme inversion. This is a meaningful visual change from the previous screenshot.

---

## Command Palette

### palette-open.png

![Command palette open](../screenshots/rpc/palette-open.png)

A white search modal is overlaid and centered on the daylight explorer. The palette shows a "Search methods..." input at the top and a list of methods with backend labels: `substrate.call`, `substrate.hash`, `substrate.hashes`, `substrate.schema`, `cone.create`, `cone.get`, `cone.list`, `cone.delete` — each tagged with `[substrate]`. The background is dimmed. This demonstrates the method-discovery palette feature.

---

### palette-close.png

![Command palette closed](../screenshots/rpc/palette-close.png)

**Minimal visual change.** The palette overlay is dismissed and the explorer view is restored. The underlying content (substrate plugin details) is unchanged from before the palette was opened — this is the expected state after dismissal.

---

## State Watch

### state-watch-before.png

![State watch — before navigation](../screenshots/rpc/state-watch-before.png)

The explorer interface with both backends active (`substrate 95ms`, `fidget-spinner 12ms`). Plugin tree and method details are displayed normally. This is the baseline frame captured immediately before a `state.watch` subscription is opened and a navigation action is fired.

---

### state-watch-after.png

![State watch — after navigation](../screenshots/rpc/state-watch-after.png)

**No visible change.** The screenshot is identical to `state-watch-before.png` — same layout, same plugin details, same backend latencies. The `state.watch` stream emitted a `view-changed` event (verified in the test), but no UI element was changed by the test itself. The navigation target was `canvas`, which produces the same visual as noted in `nav-canvas.png`.

---

## Backends

### backends-list.png

![Backends list](../screenshots/rpc/backends-list.png)

The multi-explorer view with two active backends: `substrate 74ms` and `fidget-spinner 56ms` as pill tabs at the top. The sidebar tree shows SUBSTRATE (12 children) and FIDGET-SPINNER (1 child). The main panel displays the substrate schema with its 4 methods and the `call STREAM` method signature with parameters. This is the baseline backends state used throughout this section.

---

### backends-health.png

![Backends health](../screenshots/rpc/backends-health.png)

**No visible change.** The display is identical to `backends-list.png`. The `backends.health` call returned an array of health objects (status fields), but no visual indicator updates are visible — likely because health data is reflected in the latency badges which haven't changed from this static capture.

---

### backends-methods.png

![Backends methods](../screenshots/rpc/backends-methods.png)

**No visible change.** Again identical to the two preceding backends screenshots. The `backends.methods` call returns a flat list of all registered method entries, but this is a query-only operation that triggers no visual update in the UI.

---

### backends-add.png

![Backend added](../screenshots/rpc/backends-add.png)

A meaningful visual change. A third backend tab `demo-bk ×` has appeared at the top alongside the original two. The left sidebar now shows a third section `DEMO-BK` in the backend tree. The main content area is unchanged. The × on the tab indicates this backend can be removed. This demonstrates `backends.add` reflecting immediately in the explorer UI.

---

### backends-remove.png

![Backend removed](../screenshots/rpc/backends-remove.png)

The `demo-bk` tab and its sidebar section are gone. The display reverts to just `substrate 74ms` and `fidget-spinner 56ms` — the original two-backend state. This demonstrates `backends.remove` taking effect immediately.

---

## Invoke

### invoke-call.png

![Invoke call — plexus-gamma self-loop](../screenshots/rpc/invoke-call.png)

A significant structural change. A third backend tab `plexus-gamma 4ms` has been added (plexus-gamma connected to itself as a backend). The left sidebar now includes a `PLEXUS-GAMMA` section with 9 children: ui, backends, invoke, state, screenshot, wiring, orchestration, replay, and assertion — the full RPC plugin namespace hierarchy of plexus-gamma itself. This demonstrates `invoke.call` in action and confirms the self-loop connection exposes all RPC namespaces correctly.

---

## Wiring Canvas

### wiring-add-node-1.png

![Wiring — first node added](../screenshots/rpc/wiring-add-node-1.png)

The wiring canvas with a single node added: `echo.say` positioned in the upper-left area of the canvas. A detail panel on the right shows the node's return type as `unknown` with no input parameters visible. Small circular connection ports are visible on the node. The rest of the canvas is empty with the dotted grid background.

---

### wiring-add-node-2.png

![Wiring — second node added](../screenshots/rpc/wiring-add-node-2.png)

Two nodes are now on the canvas: the original `echo.say` on the left, and a second `echo.say2` to its right. The detail panel now shows a `ref: {{echo.say}}` field indicating the second node references the first. Both nodes display connection ports. This demonstrates successful sequential node addition.

---

### wiring-set-params.png

![Wiring — params configured](../screenshots/rpc/wiring-set-params.png)

The two nodes are renamed: the first is now labeled **Source** (subtitle: `echo.say`, `hello from RPC`) and the second is **Sink** (subtitle: `echo.say`). The detail panel shows `{{Source}}` wired as input to Sink. This demonstrates parameter configuration via `wiring.setParams` — the human-readable names and the reference template are both visible.

---

### wiring-connect.png

![Wiring — connect](../screenshots/rpc/wiring-connect.png)

**No visible change.** The Source and Sink nodes are in the same configuration as `wiring-set-params.png`. The `wiring.connect` call was issued but the visual representation of the connection (if any edge line was added) is not apparent in this screenshot.

---

### wiring-routing.png

![Wiring — routing](../screenshots/rpc/wiring-routing.png)

**No visible change.** Identical to `wiring-connect.png` — Source and Sink nodes unchanged. The routing configuration action produced no distinct visual delta at capture time.

---

### wiring-autolayout.png

![Wiring — auto layout](../screenshots/rpc/wiring-autolayout.png)

The nodes have been repositioned. Source and Sink are now much closer together and shifted toward the upper portion of the canvas compared to previous frames — the auto-layout algorithm rearranged node positions for a more compact arrangement. This is a clear visual change demonstrating `wiring.autoLayout`.

---

### wiring-run.png

![Wiring — run (error)](../screenshots/rpc/wiring-run.png)

Both Source and Sink nodes now display red border outlines with error messages: *"error: RPC error -32603: Internal error"*. The detail panel also shows red error text. The workflow executed but the `echo.say` method is not available on the connected backend (fidget-spinner doesn't have an echo namespace), resulting in RPC errors on both nodes.

---

### wiring-results.png

![Wiring — results (error persists)](../screenshots/rpc/wiring-results.png)

**No visual change from previous.** Both nodes still show the same red error borders and RPC -32603 messages. This frame was captured after `wiring.getResults` — the results reflect the error state already visible.

---

### wiring-undo.png

![Wiring — undo](../screenshots/rpc/wiring-undo.png)

The red error borders and error messages are cleared. Both nodes return to normal styling with no error indicators. The detail panel is no longer visible. This demonstrates `wiring.undo` reverting the failed execution state.

---

### wiring-redo.png

![Wiring — redo](../screenshots/rpc/wiring-redo.png)

The nodes again display red error borders and the *"error: RPC error -32603: Internal error"* messages — the redo re-applied the failed execution. This demonstrates `wiring.redo` restoring the error state that was undone.

---

### wiring-export.png

![Wiring — export JSON](../screenshots/rpc/wiring-export.png)

The canvas shows Source and Sink in their error state (red borders, RPC -32603 messages visible). A minimap preview appears in the bottom-right corner, showing the node graph in miniature. No export dialog or modal appeared — the `wiring.exportJson` call returned the JSON payload but the UI did not open a file dialog. The minimap may have been triggered by the viewport update.

---

### wiring-clear.png

![Wiring — cleared](../screenshots/rpc/wiring-clear.png)

The canvas is fully empty. The placeholder text *"Click to search & add nodes, or drag from the sidebar"* is centered on the dotted grid. All nodes have been removed. The toolbar buttons and sidebar (TRANSFORMS: Extract, Template, Merge, Script; UI: Vars, Row, Col, Text, Input, Btn, Slider, Table) are visible. This correctly demonstrates `wiring.clear`.

---

### wiring-transforms-all.png

![Wiring — all transform node kinds](../screenshots/rpc/wiring-transforms-all.png)

Three transform nodes are visible on the canvas, stacked vertically: `extract` (with an `input` port), `template` (with a `slot0` port), and `script` (with an `input` port). A detail panel in the bottom-right shows configuration for a `layout: col` node with direction/row settings. A vertical list panel is visible on the far right (likely the export or node list panel). This demonstrates adding multiple distinct transform node kinds via `wiring.addTransform`.

---

## Orchestration

### orch-create.png

![Orchestration — new workflow created](../screenshots/rpc/orch-create.png)

The orchestration view with a newly created, untitled workflow. The workflow title field shows "Workflow name..." as a placeholder. A dark left sidebar shows "0 steps" with a green play button. The main canvas displays: *"No steps yet. Click + Add step to begin."* with action buttons (Run workflow, Add step, Copy JSON) in the toolbar.

---

### orch-rename.png

![Orchestration — workflow renamed](../screenshots/rpc/orch-rename.png)

The workflow is now titled **"RPC Showcase Workflow"** in the title field at the top — a meaningful visual change from the placeholder. The canvas is otherwise unchanged (still 0 steps, same empty state message). The rename is immediately visible in the title bar.

---

### orch-add-step-1.png

![Orchestration — first step added](../screenshots/rpc/orch-add-step-1.png)

The first workflow step has been added. The sidebar updates to "1 step" and the canvas shows a card labeled **Step 1** with `substrate.echo.say` as the method name (rendered in blue). Below the method name is a PARAMS section containing an empty JSON object `{}`. The empty-state message has been replaced by this step card.

---

### orch-add-step-2.png

![Orchestration — second step added](../screenshots/rpc/orch-add-step-2.png)

Two steps are now visible. The sidebar shows "2 steps" and the canvas displays Step 1 and Step 2 cards stacked vertically, connected by a thin vertical line. Step 2 uses the same `substrate.echo.say` method with empty `{}` PARAMS and a `+ add wire` button below. The connecting line between cards makes the sequential dependency explicit.

---

### orch-set-params.png

![Orchestration — step wired to previous output](../screenshots/rpc/orch-set-params.png)

Step 2's PARAMS section now shows a **WIRES** subsection with `message → step1.result` rendered in orange text. Step 1's PARAMS remain empty `{}`. This demonstrates data wiring between steps — Step 2's `message` parameter will receive `step1.result` at runtime.

---

### orch-wire.png

![Orchestration — run attempt (error)](../screenshots/rpc/orch-wire.png)

The workflow was run (via `orchestration.run`) and failed. Step 1's card is highlighted with a red/pink border and displays: *"error RPC error -32603: Internal error"* in red text. The left sidebar shows a red `fail` badge next to "2 steps". Step 2's badge reads `pending` (gray) — it was never reached because Step 1 failed.

---

### orch-run.png

![Orchestration — run state](../screenshots/rpc/orch-run.png)

**No visual change.** Identical to `orch-wire.png` — same red error border on Step 1, same fail/pending badges, same error message. This frame was captured after `orchestration.run` which had already been called; no additional visual transition occurred.

---

### orch-state.png

![Orchestration — state query](../screenshots/rpc/orch-state.png)

**No visual change.** Again identical to the two preceding orchestration screenshots. The `orchestration.state` call is a query (returns step statuses), not a mutating action, so no visual update is expected.

---

## Replay

### replay-list.png

![Replay — history list](../screenshots/rpc/replay-list.png)

The multi-explorer view with the `echo` method selected from the SUBSTRATE backend. The right sidebar shows a **HISTORY** panel with the message *"No invocations recorded yet."* The center panel shows echo method details with parameter input fields. This is the initial state — no invocations have been made yet so the history is empty.

---

### replay-clear.png

![Replay — cleared](../screenshots/rpc/replay-clear.png)

**No visible change.** The display is identical to `replay-list.png` — same echo method interface and the same *"No invocations recorded yet."* message in the HISTORY panel. The `replay.clear` call was issued but since the test did not record any invocations before clearing, the HISTORY panel was already empty and remains empty.

---

## Assertions

### assertion-add.png

![Assertions — test case added](../screenshots/rpc/assertion-add.png)

The explorer view with the echo method selected. Small icon buttons (copy/test controls) have appeared in the top-right corners of each method section (`echo.echo` and `echo.once`). These controls indicate that the assertion suite is active. The HISTORY panel on the right is no longer visible, replaced by a grayed-out area — possibly because the assertion panel replaced it.

---

### assertion-list.png

![Assertions — list](../screenshots/rpc/assertion-list.png)

**No visible change.** The display is identical to `assertion-add.png` — same method panels with the same icon buttons, same layout. The `assertion.list` call is a query and produces no visual update.

---

### assertion-run.png

![Assertions — run test](../screenshots/rpc/assertion-run.png)

**No visible change.** Identical to the two preceding assertion screenshots. Running a specific assertion test via `assertion.runTest` did not produce a visible result indicator, pass/fail badge, or any other visual feedback in this captured frame.

---

### assertion-runall.png

![Assertions — run all tests](../screenshots/rpc/assertion-runall.png)

**No visible change.** Same state as the previous three assertion screenshots — method panels with icon buttons, empty parameter fields, no execution feedback visible. Running all assertion tests (`assertion.runAll`) produced no visual UI update at the time of screenshot capture.

---

## Notes on Silent Screenshots

Several actions in this guide produced no visible UI change:

| Screenshot | Why nothing changed |
|---|---|
| `nav-canvas.png` | `ui.navigate` fired but canvas tab renders same explorer layout |
| `state-watch-after.png` | `state.watch` emits events internally; no UI mutation in the test |
| `backends-health.png` | Query-only; health data already reflected in latency badges |
| `backends-methods.png` | Query-only; returns data, no visual mutation |
| `wiring-connect.png` | Connection may be logical (via `ref:` template), not a drawn edge |
| `wiring-routing.png` | Routing mode change, no visible edge lines in this graph |
| `wiring-results.png` | Already showing error state; results confirm same state |
| `orch-run.png` | Already in failed state from previous screenshot |
| `orch-state.png` | Query-only; no visual mutation |
| `replay-clear.png` | History was already empty; nothing to clear visually |
| `assertion-list.png` | Query-only |
| `assertion-run.png` | No visible pass/fail indicator rendered in captured frame |
| `assertion-runall.png` | Same as above |
