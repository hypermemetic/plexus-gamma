# plexus-gamma

## After completing any task
Always commit the changes when done. Stage only the modified files and write a concise commit message following the existing style (`feat(wiring): ...`, `fix(wiring): ...`, etc.).
